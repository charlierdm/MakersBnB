CREATE TABLE spaces (id SERIAL PRIMARY KEY, name VARCHAR(500), description VARCHAR(5000), user_id VARCHAR(200), price VARCHAR(100), available_from DATE, available_to DATE);


# jack, nico, kasey ....the following have been added to spaces table (4/3) available_from DATE, available_to DATE
